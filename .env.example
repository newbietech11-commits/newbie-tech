# Leave blank to call the backend at the same origin (recommended behind a
# reverse proxy where /api is routed to the backend container).
# Set to e.g. http://localhost:8080 only for local dev without the Vite proxy.
VITE_API_BASE=
